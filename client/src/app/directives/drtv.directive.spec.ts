import { DrtvDirective } from './drtv.directive';

describe('DrtvDirective', () => {
  it('should create an instance', () => {
    const directive = new DrtvDirective();
    expect(directive).toBeTruthy();
  });
});
